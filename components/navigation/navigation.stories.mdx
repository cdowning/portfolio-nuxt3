<!-- navigation.stories.mdx -->

import {
    Canvas,
    Meta,
    Story,
    ArgsTable,
    Description,
} from '@storybook/addon-docs';

import Navigation from './navigation.vue';

export const directionOptions = ['horizontal', 'vertical'];

<Meta
    title="Components/Navigation"
    argTypes={{
        direction: {
            description: 'Direction of links - horizontal or vertical',
            options: directionOptions,
            table: {
                type: { summary: 'string' },
                defaultValue: { summary: 'horizontal' },
            },
            control: {
                type: 'select',
            },
        },
    }}
></Meta>

export const Template = (args, { argTypes }) => ({
    props: Object.keys(argTypes),
    components: { Navigation },
    template: `
        <Navigation v-bind="$props">
            <a class="mx-3">
                <Icon class="mx-2" icon="female" />
                About
            </a>
            <a class="mx-3">
                <Icon class="mx-2" icon="briefcase" />
                Experience
            </a>
            <a class="mx-3">
                <Icon class="mx-2" icon="envelope" />
                Contact
            </a>
        </Navigation>
    `,
});

# Navigation

## Overview

A menu is a navigatable contextual list of items that can be selected.

<Description of={Navigation} />

<Canvas>
    <Story
        name="Overview"
        args={{
            direction: 'horizontal',
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>

<ArgsTable story="Overview" />
