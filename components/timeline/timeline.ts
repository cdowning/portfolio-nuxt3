const Timeline = defineComponent({
    name: 'Timeline',
    props: {
        isVertical: {
            type: Boolean,
            default: true,
        },
    },
    setup() {},
});

export default Timeline;
