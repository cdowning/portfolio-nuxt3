const TimelineDetails = defineComponent({
    name: 'TimelineDetails',
    props: {
        borderColor: {
            type: String,
            default: '',
        },
    },
    setup(props) {},
});

export default TimelineDetails;
